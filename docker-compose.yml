version: '3.8'

services:
  # Frontend Service
  frontend:
    build:
      context: ./packages/frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./packages/frontend:/app
      - /app/node_modules
    env_file:
      - .env
    networks:
      - app-network

  # API Gateway
  api-gateway:
    image: kong:latest
    ports:
      - "8000:8000"
      - "8443:8443"
      - "8001:8001"
      - "8444:8444"
    environment:
      KONG_DATABASE: 'off'
      KONG_DECLARATIVE_CONFIG: /etc/kong/kong.yml
      KONG_PROXY_ACCESS_LOG: /dev/stdout
      KONG_ADMIN_ACCESS_LOG: /dev/stdout
      KONG_PROXY_ERROR_LOG: /dev/stderr
      KONG_ADMIN_ERROR_LOG: /dev/stderr
      KONG_ADMIN_LISTEN: 0.0.0.0:8001
    volumes:
      - ./kong.yml:/etc/kong/kong.yml
    networks:
      - app-network

  # User Service
  user-service:
    build:
      context: ./packages/user-service
      dockerfile: Dockerfile
    env_file:
      - .env
    depends_on:
      - postgres
    networks:
      - app-network

  # Generation Service
  generation-service:
    build:
      context: ./packages/generation-service
      dockerfile: Dockerfile
    env_file:
      - .env
    depends_on:
      - postgres
      - redis
    networks:
      - app-network

  # AI Worker
  ai-worker:
    build:
      context: ./packages/ai-worker
      dockerfile: Dockerfile
    volumes:
      - ./packages/ai-worker:/app
      - model-cache:/root/.cache/huggingface
      - generated-assets:/app/assets
    env_file:
      - .env
    depends_on:
      - postgres
      - redis
    networks:
      - app-network

  # Profiling Service
  profiling-service:
    build:
      context: ./packages/profiling-service
      dockerfile: Dockerfile
    env_file:
      - .env
    depends_on:
      - postgres
    networks:
      - app-network

  # Asset Server
  asset-server:
    build:
      context: ./packages/asset-server
      dockerfile: Dockerfile
    ports:
      - "8082:8082"
    volumes:
      - generated-assets:/app/assets
    networks:
      - app-network

  # Community Service
  community-service:
    build:
      context: ./packages/community-service
      dockerfile: Dockerfile
    env_file:
      - .env
    depends_on:
      - postgres
    networks:
      - app-network

  # PostgreSQL Database
  postgres:
    image: postgres:15
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=db
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - app-network

  # Redis Cache
  redis:
    image: redis:7
    ports:
      - "6379:6379"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  postgres-data:
  model-cache:
  generated-assets: